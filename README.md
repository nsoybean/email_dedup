# Email Ingestion and Deduplication

Prototype implementation for the email ingestion and deduplication assignment.

The project is being built incrementally. See `build_interactive_plan.md` for the developer
learning workflow.
