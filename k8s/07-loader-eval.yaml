# Applied by `make ingest-eval` — loads image-baked data/eval.
apiVersion: batch/v1
kind: Job
metadata:
  name: loader-eval
  namespace: email-dedup
  labels:
    app: loader
    corpus: eval
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: loader
        corpus: eval
    spec:
      restartPolicy: Never
      containers:
        - name: loader
          image: email-dedup:local
          imagePullPolicy: Never
          command:
            - python
            - scripts/load_directory.py
            - data/eval
            - --base-url
            - http://api:8000
          envFrom:
            - configMapRef:
                name: email-dedup-config
